const apiService = require('./api');

const {BOTS_COUNT} = process.env;

const poasibleNames = ['john', 'roz', 'sara', 'anonymous', 'micheal'];


const importBots = (count) => {
    
}

const start = () => {

}